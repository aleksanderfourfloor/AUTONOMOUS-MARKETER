NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Server-side SQLite database file used by the Next.js app (Node runtime only).
# This does NOT run in the browser.
SQLITE_PATH=./.data/frontend.sqlite
